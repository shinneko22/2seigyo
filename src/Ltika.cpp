/*#include <mbed.h>
using namespace rtos::ThisThread;
using ThisThread::sleep_for;

DigitalOut myled(LED1);

int main() {
    while(1) {
        myled = 1;
        sleep_for(250);        
        myled = 0;
        sleep_for(250);
    }
}*/